name: Check PR Title
on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main

permissions:
  pull-requests: read

jobs:
  Check-PR-Title:
    name: Validate PR Title
    runs-on: [self-hosted, solinas]
    steps:
      - name: Check PR title
        uses: cap/.github/.github/actions/check-pr-title@a10b981add83215f0e5fb043670ac1547950199b # main
