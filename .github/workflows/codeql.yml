name: CodeQL

on:
  push:
    branches: [main]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@613c5b00ff920ae7d880af4534c83a727900da9e # main
    secrets: inherit
    with:
      use-sap: true
