name: CodeQL

on:
  push:
    branches: [main]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@5507e8e09411e392cc3e81bacd6773358c1d6b24 # main
    secrets: inherit
    with:
      use-sap: true
