name: CodeQL

on:
  push:
    branches: [main]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@f52abfb84ed361d807bc7c3613df7955bc719275 # main
    secrets: inherit
    with:
      use-sap: true
