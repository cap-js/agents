name: CodeQL

on:
  push:
    branches: [main]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@931efef2f74232949efd47ea5fa3d36876d140c2 # main
    secrets: inherit
    with:
      use-sap: true
