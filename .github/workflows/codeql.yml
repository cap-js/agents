name: CodeQL

on:
  push:
    branches: [main]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@725cb63379e2e73f163abfa7d77f12ec7c1e1a81 # main
    secrets: inherit
    with:
      use-sap: true
