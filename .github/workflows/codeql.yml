name: CodeQL

on:
  push:
    branches: [main]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  codeql:
    uses: cap/.github/.github/workflows/codeql.yml@20d670f8fe531e96ac02e73d09249106b36339ac # main
    secrets: inherit
    with:
      use-sap: true
