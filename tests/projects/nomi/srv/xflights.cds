/**
 * Include the xflights data service in this application.
 * The @capire/xflights package provides Airlines, Airports, FlightConnections,
 * Flights, Supplements, and FlightBookings — served in-process from its own data.
 */
using from '@capire/xflights';
